{
  "name": "cadp-release-notes",
  "version": "1.0.0",
  "private": true,
  "description": "Automated, always-current CADP release-notes service (bug IDs from OpenProject, narrative from docs/changes, mail-driven version rollover). Deploys to Google Cloud Run.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "build:live": "node scripts/build-live.js",
    "build:mail": "node scripts/build-live.js --final && node scripts/pdf-edge.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {}
}
