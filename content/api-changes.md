# API Changes (optional manual manifest)

Prefer `QA-Release-Note: api` in the relevant `docs/changes` entry for API changes tied to a commit.
Use this file only for API notes with no code-change entry. Empty table → the section shows "None".

| API Name | Type | Description |
|---|---|---|
